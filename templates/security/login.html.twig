{% extends 'base.html.twig' %}

{% block title "Snowtricks - Se connecter" %}

{% block body %}
<div class="banner_img">
</div>
<div class="container mt-4">
	<h1>Se connecter</h1>  
</div>

<div class="container">
		{{ form_start(form) }}
			{{ form_widget(form) }}
			<button class="btn btn-primary">Éditer</button>
		{{ form_end(form) }}
</div>
{% endblock %}